jyrajan@gmail.com

yum update

yum install

read up on difference of ebs, efs and s3 (when to use each of it)
VPC DO NOT CROSS REGIONS, (but span all availability zone)
subnet must be in availability zone. subnet do not cross availability zone

CIDR
http://cidr.xyz/
10.0.0.0/24 (supports 256 hostnames class C)
1 subnet can 10.0.0.0/25 (for ip addresses 10.0.0.0 to 10.0.0.127)
another subnet can 10.0.0.128/25 (for ip addresses 10.0.0.129 to 10.0.0.254)

largest CIDR block in aws is /16
smallest CIDR block in aws is /28

there are 5 IPs ALWAYS USE BY AWS
so if 10.0.0.0/28, only have 11 hostnames available (not 16)

example
172.31.0.0/16
need 16 subnets

look at thrid position, 2^1 = only 2, so not enough
if need 16 subnets, need 2^4 = 16, SO FIRST SUBNET IS 172.31.0.0
2ND subnet is 172.31.16.0
3rd subnet is 172.31.32.0

so steal 4 subnet from third position
2^4 = 16



example
public 10.0.1.0/24
private 10.0.2.0/24


VPC
2 SUBNET
ROUTING TABLE
FOR PUBLIC, CREATE INTERNET GATEWAY
FOR PRIVATE, NAT GATEWAY (CUZ NO PUBLIC IP)
SG

yum install httpd

amazon-linux-extras install php7.2

wget https://dev.mysql.com/get/mysql57-community-release-el7-11.noarch.rpm

yum localinstall mysql57-community-release-el7-11.noarch.rpm

yum install mysql-community-server

systemctl status mysqld.service

systemctl start mysqld.service

grep 'temporary password' /var/log/mysqld.log
TIFiLzqx&0qO

j+y7i!w(iVna

mysql_secure_installation




Wordpress

wget http://wordpress.org/latest.tar.gz

tar -xzvf latest.tar.gz

ip/wordpress/wp-admin/install.php

ip/wordpress/wp-login.php


cd /var/www/html
wget https://www.phpmyadmin.net/downloads/phpMyAdmin-latest-all-languages.tar.gz
mkdir phpMyAdmin && tar -xvzf phpMyAdmin-latest-all-languages.tar.gz -C phpMyAdmin --strip-components 1
rm phpMyAdmin-latest-all-languages.tar.gz

ip/phpMyAdmin


cd /var/www/html/phpMyAdmin
cp config.sample.inc.php config.inc.php
vi config.inc.php

/**
 * First server
 */
$i++;
/* Authentication type */
$cfg['Servers'][$i]['auth_type'] = 'cookie';
$cfg['Servers'][$i]['connect_type'] = 'tcp';
/* Server parameters */
$cfg['Servers'][$i]['host'] = '10.0.2.99';
$cfg['Servers'][$i]['port'] = '3306';
$cfg['Servers'][$i]['user'] = 'root';
$cfg['Servers'][$i]['password'] = 'P@ssw0rd';
$cfg['Servers'][$i]['extension'] = 'mysqli';



select * from mysql.user;

GRANT  

GRANT ALL PRIVILEGES ON * . * TO 'user1'@'10.10.8.196' IDENTIFIED BY 'PASSW';
FLUSH PRIVILEGES ;



https://www.whizlabs.com/login/